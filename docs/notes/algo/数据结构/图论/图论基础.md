# 图论基础

> 图论（Graph theory）是数学的一个分支，图是图论的主要研究对象，图（Graph）是有由若干个给定点以及连接两顶点的边所构成的图形，这种图形通常用来描述某些事物之间的特定关系，顶点用来代表事物，连接两顶点的边则用于表示两个事物具有这种关系。

##  前言

<img src="https://img.doooor.com/img/forum/202410/05/012348jz1ql2qndpdruk2q.jpg" alt="社交网络图示例" style="zoom:80%;" />

对于这么一张复杂且繁多的社交关系图，在电脑中会用什么数据结构去保存这个网络呢？

answer：图结构

### 什么是图

图是由一系列顶点和若干个连接顶点集合内两个顶点的边组成的数据结构，数学意义上的图，指的是由一系列点和边构成的集合，这里只考虑有限集。通常使用G(V,E)表示一个图结构，其中V为点集，E为边集

说到边，你肯定会问，边可以连接两个顶点，那么这两个顶点的关系可能是双向也可能是单向的啊？是的，所以引入两个概念--有向边和无向边

简单来理解，有向边可以理解为单向的关系，由A指向B，也就代表了A跟B有关系，但B跟A没有关系，反之，无向边则为简单的双向联系。

#### 图示

无向图

![img](https://pic2.zhimg.com/v2-7f615addefe05d7dab96cdb408aff6a9_1440w.jpg)

有向图

![img](https://pic1.zhimg.com/v2-c690dfd68585a94d9858ab70a67bc0ac_1440w.jpg)

### 基础概念

#### 图的分类

![img](https://pic3.zhimg.com/v2-c1d5f555db014b68f850c002d9b907d6_1440w.jpg)

![img](https://pica.zhimg.com/v2-23db6d1fc0f5225d0c6896bc306f8cc8_1440w.jpg)

#### 度

##### 定义

在无向图中，顶点的度是指从某个顶点连出的边数，例如下图

![img](https://picx.zhimg.com/v2-eaffd3db8f28e84b624a8acb3004d6db_1440w.jpg)

a的度数为4，b的度数为3

而在有向图中，我们引入出度和入度两个概念，入度即为已该顶点为终点的有向边的数量，出度是指以该顶点为起点的有向边的数量，注意，对于有向边不可单独说度的概念。

![img](https://pica.zhimg.com/v2-e5ed1cdffa42db803338d8e18a28165e_1440w.jpg)

在这张图中，a的入度为1，出度为3，c的入度为2，出度为2

##### 性质

在无向图或者有向图中，顶点的度数总和为边数的两倍
$$
|E|=\frac{1}{2}\sum_{i=1}^{n}deg(u_i)
$$
而对于有向图，出度等于入度。

## 正题

### 度数统计

无向图度数

```c++
#include <iostream>
using namespace std;
int deg[105];
int main() {
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        deg[u]++;
		deg[v]++;
    }
    for (int i = 1; i <= n; i++) {
        cout << deg[i] << " ";
    }
    return 0;
}
```

有向图度数

```c++
#include <iostream>
using namespace std;
int outdeg[105], indeg[105];
int main() {
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        outdeg[u]++;
        indeg[v]++;
    }
    for (int i = 1; i <= n; i++) {
        cout << outdeg[i] << " " << indeg[i] << endl;
    }
    return 0;
}
```

### 图的存储

> 图的存储有三种方法，分别为邻接表，邻接矩阵，链式前向星

#### 邻接矩阵

邻接矩阵，顾名思义，肯定会用二维数组去存一个矩阵，对于两个顶点，如果两点存在边，则数组标记为1，否则为0，下面看看例子

![img](https://pic1.zhimg.com/v2-440edf998ddc1c389545a48ff9f0f240_1440w.jpg)

一个图的邻接矩阵是唯一的，矩阵的大小只与顶点个数N有关，是一个 *N×N* 的矩阵。前面我们已经介绍过，在无向图里，如果顶点 *vi* 和 *vj* 之间有边，则可认为顶点 *vi* 到 *vj* 有边，顶点 *vj* 到 *vi* 也有边。对应到邻接矩阵里，则有 *A[i] [j]=A[j] [i]=1*　。因此我们可以发现，无向图的邻接矩阵是一个对称矩阵。

在邻接矩阵上，我们可以直观地看出两个顶点之间是否有边（或弧），并且很容易求出每个顶点的度，入度和出度。

这里我们以 *G1* 为例，演示下如何利用邻接矩阵计算顶点的入度和出度。顶点的出度，即为邻接矩阵上点对应行上所有值的总和，比如顶点1出度即为0+1+1+1=3；而每个点的入度即为点对应列上所有值的总和，比如顶点3对应的入度即为1+0+0+1=2。

知道这些，让我们看看代码该如何实现吧

##### **初始化**

初始化的过程很简单，只需要把数组初始化为0即可。可以借助`memset`来快速地将一个数组中的所有元素都初始化为0。（其实定义成全局变量就行了……）

```text
memset(G, 0, sizeof(G));
```

**注意，`memset`只能用来初始化0和 -1，并且需要加上头文件`cstring`。**

上面的代码等价于：

```text
for (int i = 0; i < N1; i++) { // N1 为数组第一维大小
    for (int j = 0; j < N2; j++) { // N2 为数组第二维大小
        G[i][j] = 0;
    }
}
```

当然我们平常使用邻接矩阵的时候下标只用 *1* 到 *n* 或者 *0* 到*n−1* *（这个看题目中点的编号）*。

##### **插入边**

如果插入一条无向边 *(u,v)* ，只需要

```text
G[u][v] = 1;
G[v][u] = 1;
```

也可以写成`G[u][v] = G[v][u] = 1;`。
如果插入一条有向边 *(u,v)* ，只需要`G[u][v] = 1;`。

##### **访问边**

如果`G[u][v] = 1`，说明有一条从 *u* 到 *v* 的边，否则没有从 *u* 到 *v* 的边。

##### 代码

```c++
#include <iostream>
using namespace std;
const int maxn = 105;
int G[maxn][maxn];
int main() {
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
		G[u][v] = G[v][u] = 1;
    }
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            cout << G[i][j] << " ";
        }
        cout << endl;
    }
    return 0;
}
```

### 邻接表

邻接表的思想在于，对于图中每一个顶点，用一个数组记录这个点和那些点相连，由于相连的点会动态变化，所以对于每一个点，用vector记录

也就是对于每个点，我们都用一个`vector`来记录这个点和哪些点相连。比如对于一张有 10 个点的图，`vector<int> G[11]`就可以用来记录这张图了。对于一条从 *a* 到 *b* 的有向边，我们通过`G[a].push_back(b)`就可以把这条边添加进去；如果是无向边，则需要在`G[a].push_back(b)`的同时`G[b].push_back(a)`。

<img src="https://pica.zhimg.com/v2-09bd69e515de6332571f48cfd2b27cac_1440w.jpg" alt="img" style="zoom:80%;" />

#### **优点**

1. 节省空间：当图的顶点数很多、但是边的数量很少时，如果用邻接矩阵，我们就需要开一个很大的二维数组，最后我们需要存储 n*n 个数。但是用邻接表，最后我们存储的数据量只是边数的两倍。
2. 可以记录重复边：如果两个点之间有多条边，用邻接矩阵只能记录一条，但是用邻接表就能记录多条。虽然重复的边看起来是多余的，但在很多时候对解题来说是必要的。

#### **缺点**

当然，有优点就有缺点，用邻接表存图的最大缺点就是随机访问效率低。比如，我们需要询问点 *a* 是否和点 *b* 连，我们就要遍历`G[a]`，检查这个`vector`里是否有 *b* 。而在邻接矩阵中，只需要根据`G[a][b]`就能判断。

因此，我们需要对不同的应用情景选择不同的存图方法。如果是稀疏图（顶点很多、边很少），一般用邻接表；如果是稠密图（顶点很少、边很多），一般用邻接矩阵。

> 所以说，点数大概超过5000时，采用邻接表而非邻接矩阵！

#### 代码

```c++
#include <iostream>
#include <vector>
using namespace std;
const int maxn = 105;
vector<int> G[maxn];
int main() {
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        G[u].push_back(v);
        G[v].push_back(u);
    }
    for (int i = 1; i <= n; i++) {
        cout << i << " : ";
        for (int j = 0; j < G[i].size(); j++) {
            cout << G[i][j] << " ";
        }
        cout << endl;
    }
    return 0;
}
```

### 扩展（带权值的存储）

#### 邻接矩阵

```c++
#include <iostream>
#include <cstring>
using namespace std;
const int maxn = 105;
int G[maxn][maxn];
int sum[maxn];
int main() {
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v, w;
        cin >> u >> v >> w;
		G[u][v] = w;
    }
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            cout << G[i][j] << " ";
        }
        cout << endl;
    }
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            sum[i] += G[i][j];
        }
    }
    for (int i = 1; i <= n; i++) {
        cout << sum[i] << " ";
    }
    return 0;
}
```

---

#### 邻接表

```c++
#include <iostream>
#include <vector>
using namespace std;
const int maxn = 105;
struct node {
    int v;
    int w;
};
vector<node> G[maxn];
void insert(int u, int v, int w) {
    node temp;
    temp.v = v;
    temp.w = w;
    G[u].push_back(temp);
}
int main() {
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v, w;
        cin >> u >> v >> w;
        insert(u, v, w);
    }
    for (int i = 1; i <= n; i++) {
        for (int j = 0; j < G[i].size(); j++) {
			cout << i << " " << G[i][j].v << " " << G[i][j].w << endl;
        }
    }
    return 0;
}
```

Ps.这里的add操作也可以用pair，请看：

```c++
vector<pair<int,int> >g[n+1];
for(int i=1;i<=m;i++){
    int u,v,w;
    cin>>u>>v>>w;
    g[u].push_back({v,w});
    g[v].push_back({u,w});
}
```

---

